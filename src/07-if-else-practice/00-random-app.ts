import { print, random } from "introcs";

export let main = async () => {

    // Choice is assigned a random number between 0 and 99
    let choice = random(0, 99);

    // TODO: Print the choice variable and refresh your browser

};

main();