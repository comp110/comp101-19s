import { random } from "introcs";

// Initialize variables connected to HTML objects
let input = document.getElementById("input") as HTMLInputElement;
let submit = document.getElementById("submit") as HTMLButtonElement;
// TODO #1: Initialize the response variable

// Generate a random number
let answer = random(1, 100);

export let main = async () => {
    // TODO #0: Assign a color to the background to be magenta

    // TODO #3: Assign submit's onclick property to checkGuess

};

// TODO #2: Establish a checkGuess function

main();