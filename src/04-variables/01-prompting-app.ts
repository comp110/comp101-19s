import { print, promptString } from "introcs";

export let main = async () => {

    let name = await promptString("What is your name?");

    // TODO: Using concatenation, print an uplifting message to the user.

};

main();