import { print } from "introcs";

export let main = async () => {

    let a: number[] = [2, 3, 5, 7, 11, 13];
    let result: number = 0;

    let i: number = 0;

    // TODO: Write a while loop here
    // It should:
    // 1. Loop while i is less than a's length
    // 2. Inside the loop:
    // 2.1 Increase the result variable by a[i]'s value
    // 2.2 Increment i by 1

    // TODO: After the loop completes, print result

};

main();